from LinkedList import *


partition (lList, val):
	less = LinkedList()
	greater = LinkedList()
	
	# add elements as read to the greater and lesser linked lists
	# combine the linked lists for the ending thing. 

ll = LinkedList()
ll.generate(10, 20, 30, 40, 50, 60, 70, 80 90)
print(ll)
partition(ll, ll.head.value)
print(ll)